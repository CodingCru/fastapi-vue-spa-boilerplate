name: CI workflow

on:
    push:
        branches: [main]
    pull_request:

jobs:
    backend:
        name: Backend(Python)
        runs-on: ubuntu-latest
        defaults:
            run:
                working-directory: ./backend
        steps:
            
        
    frontend:
        name: Frontend(Vue)
        runs-on: ubuntu-latest
        defaults:
            run:
                working-directory: ./frontend
        steps: